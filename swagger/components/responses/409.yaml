description: Conflict error
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 409
        message:
          type: string
          example: ConflictError
        data:
          type: object
          properties:
            message:
              type: string
              example: 'Email in use'
